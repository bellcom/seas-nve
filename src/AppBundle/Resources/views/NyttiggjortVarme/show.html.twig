{% extends '::base.html.twig' %}

{% block body -%}
    <h1>NyttiggjortVarme</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Faktor</th>
                <td>{{ entity.faktor }}</td>
            </tr>
            <tr>
                <th>Titel</th>
                <td>{{ entity.titel }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('nyttiggjortvarme') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('nyttiggjortvarme_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
