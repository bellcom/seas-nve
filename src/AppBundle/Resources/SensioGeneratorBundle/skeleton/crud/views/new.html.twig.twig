{% block extends %}
  {{ "{% extends 'AppBundle::base.html.twig' %}" }}
{% endblock extends %}

{% block body %}
  {{ "{% block body -%}" }}
  <div class="row heading">
    <div class="col-md-9">
      <h1>{{ "{{ '" }}{{ entity | replace({'\\' : "."}) | lower }}{{ ".actions.add' | trans }}" }}</h1>
    </div>
    <div class="col-md-3 text-right">
    </div>

  </div>

  {{ '{{ form(form) }}' }}

  {% set hide_edit, hide_delete = true, false %}
  {{ "{% endblock %}" }}
{% endblock body %}
