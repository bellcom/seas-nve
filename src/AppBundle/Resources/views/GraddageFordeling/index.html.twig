{% extends 'AppBundle::base.html.twig' %}


  {% block body -%}

    <div class="row heading">
      <div class="col-md-9">
        <h1>{{ 'graddagefordeling.labels.plural' | trans }}</h1>
      </div>
      <div class="col-md-3 text-right">
        <a class="btn btn-default" href="{{ path('graddage_new') }}">
          {{ icon('plus') }} {{ 'graddagefordeling.actions.add' | trans }}
        </a>
      </div>

    </div>

    {% include 'AppBundle:GraddageFordeling:_index_table.html.twig' with { 'can_edit':true } %}

  {% endblock %}
