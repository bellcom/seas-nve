{% extends 'AppBundle::base.html.twig' %}

{% block body -%}
  <h1>{{ 'user.actions.edit' | trans }}</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('user') }}">
            Back to the list
        </a>
    </li>

</ul>
{% endblock %}
