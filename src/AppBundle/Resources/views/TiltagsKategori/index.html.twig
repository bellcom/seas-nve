  {% extends 'AppBundle::base.html.twig' %}


  {% block body -%}

  <div class="row heading">
    <div class="col-md-9">
        <h1>{{ 'tiltagskategori.labels.plural' | trans }}</h1>
    </div>
    <div class="col-md-3 text-right">
              <a class="btn btn-default" href="{{ path('tiltagskategori_new') }}">
          {{ icon('plus') }} {{ 'tiltagskategori.actions.add' | trans }}
        </a>
          </div>

  </div>


  <table class="table table-condensed">
    <thead>
    <tr>
        {{ helper.header('appbundle.tiltagskategori.navn') }}
      {{ helper.header() }}
    </tr>
    <tr>
        {{ helper.header_unit('appbundle.tiltagskategori.navn') }}
      {{ helper.header_unit() }}
    </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
    <tr>
        <td>{{ entity.navn }}</td>
      <td class="text-right">    <a class="btn btn-default btn-xs" href="{{ path('tiltagskategori_edit', { 'id': entity.id }) }}">{{ 'common.edit' | trans }}</a>

      </td>

    </tr>
    {% endfor %}
    </tbody>
  </table>

  {% endblock %}
