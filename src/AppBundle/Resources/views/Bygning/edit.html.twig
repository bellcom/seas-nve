{% extends 'AppBundle::base.html.twig' %}

{% block body -%}
  <div class="row heading">
    <div class="col-md-8">
      <h1>{{ 'bygninger.actions.edit' | trans }}</h1>
    </div>
    <div class="col-md-4 text-right">
      {% if is_granted('ROLE_BYGNING_EDIT') %}
        {{ form(delete_form) }}
      {% endif %}
    </div>
  </div>

  {% include 'AppBundle:Bygning:_editForm.html.twig' %}

{% endblock %}
