{% extends 'AppBundle::base.html.twig' %}

{% if heading is not defined %}
  {% set heading = 'bilag.actions.edit' %}
{% endif %}

{% block body -%}
  <div class="row heading">
    <div class="col-md-8">
      <h1>{{ 'bilag.actions.edit' | trans }}</h1>
    </div>
    <div class="col-md-4 text-right">
      {% if is_granted('RAPPORT_EDIT', rapport) %}
        {{ form(delete_form) }}
      {% endif %}
    </div>
  </div>

  {% include 'AppBundle:RapportBilag:_form.html.twig' %}

{% endblock %}
