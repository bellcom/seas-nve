{% extends 'AppBundle::base.html.twig' %}

  {% block body -%}
    <div class="row heading">
      <div class="col-md-9">
        <h1>{{ 'elokategori.actions.edit' | trans }}</h1>
      </div>
      <div class="col-md-3 text-right">
        {{ form(delete_form) }}
      </div>

    </div>

    {{ form_start(edit_form) }}
    {{ form_row(edit_form.navn) }}
    {{ form_row(edit_form.totalVarmeforbrug) }}
    {{ form_row(edit_form.totalElforbrug) }}
    {{ form_row(edit_form.andelVarmeGUFFaktor) }}

    {{ form_rest(edit_form) }}

    {{ form_end(edit_form) }}

  {% endblock %}
