{% extends 'AppBundle::base.html.twig' %}

{% block body -%}

  <div class="row heading">
    <div class="col-md-9">
      <h1>{{ 'komponent.actions.edit' | trans }}</h1>
    </div>
    <div class="col-md-3 text-right">
      {% if delete_form is defined %}{{ form(delete_form) }}{% endif %}
    </div>
  </div>

  {{ form(edit_form) }}

{% endblock %}
