{% extends 'AppBundle::base.html.twig' %}

{% block body -%}
  <div class="row heading">
    <div class="col-md-9">
      <h1>{{ 'rapportsektion.actions.edit' | trans }}</h1>
    </div>
    <div class="col-md-3 text-right">
      {% if delete_form is defined %}{{ form(delete_form) }}{% endif %}
    </div>
  </div>

  {% include 'AppBundle:RapportSektion:_form.html.twig' %}

{% endblock %}
